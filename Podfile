# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'JobSeek' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for JobSeek
  pod "TPKeyboardAvoiding" #manages keyboard behaviour scroll, next, resign etc..

end
